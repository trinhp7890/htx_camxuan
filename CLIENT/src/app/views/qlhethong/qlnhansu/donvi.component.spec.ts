import { ComponentFixture, TestBed } from '@angular/core/testing';

import {  } from './donvi.component';

describe('DonviComponent', () => {
  
  beforeEach(async () => {
    await TestBed.configureTestingModule({
    
    })
    .compileComponents();
  });

  beforeEach(() => {
   
  });

  it('should create', () => {    
  });
});
